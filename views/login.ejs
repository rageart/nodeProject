<!DOCTYPE html>
<html lang="ko">

<head>
    <% include ./common/head %>
</head>

<body class="frame-body" style="background-color:rgb(221, 221, 221);">
    <div class="page-loading">
        <div class="loading">
            <p class="">Loading...</p>
        </div>
        <div class="overlay"></div>
    </div>
    <!-- /page-loading -->

    <div class="wrap">
        <div class="frame-container">
            <section class="page-content">
                <form class="form-signin" action="/login" method="post">
                    <div class="heading">
                        <h2 class="form-signin-heading">LGD Direct cap</h2>
                        <p>LG 디스플레이 직캠</p>
                    </div>
                    <!-- /heading -->
                    <div class="form-group">
                        <label class="sr-only" for="inputEmail">아이디</label>
                        <input class="form-control" id="inputEmail" name="inputEmail" autofocus="" required="" type="email" placeholder="아이디">
                    </div>
                    <!-- /form-group -->
                    <div class="form-group">
                        <label class="sr-only" for="inputPassword">비밀번호</label>
                        <input class="form-control" id="inputPassword" name="inputPassword" required="" type="password" placeholder="비밀번호">
                    </div>
                    <!-- /form-group -->
                    <button class="btn btn-lg btn-lgd btn-block" type="submit">Sign In</button>
                    <div class="bottom">
                        <div class="checkbox checkbox-uxp checkbox-inline">
                            <input id="saveID" type="checkbox" value="remember-me">
                            <label for="saveID">아이디저장</label>
                        </div>
                        <div class="find">
                            <a href=""><span class="fa fa-lock"></span> 아이디/비밀번호 저장</a>
                        </div>
                    </div>
                    <!-- /bottom -->
                </form>
                <!-- /form-signin -->

            </section>
        </div>
    </div>
    <!-- /wrap -->
    <% include ./common/scripts %>
        <script src="/javascript/c_login.js"></script>
        <!-- 페이지 스크립트 -->
        <script>

            $(function() {
                // UI init
                uxp.searchBox();

            });
            console.log(<%=data.error%>);
            if (<%=data.error%> == "1") {
                bootbox.alert("DATABASE 접속 오류");
            } else if (<%=data.error%> == "2") {
                bootbox.alert("등록되지 않은 아이디입니다. 다시 확인하세요");
            } else if (<%=data.error%> == "3") {
                bootbox.alert("아이디 혹은 비밀번호를 다시 확인하세요.");
            } else if (<%=data.error%> == "4") {
                bootbox.alert("비밀번호 5회 실패로 로그인 할 수 없습니다.\n관리자에게 문의하세요  ");
            } else if (<%=data.error%> == "5") {
                bootbox.confirm("비밀번호 변경기간이 90일 이상되어 변경해야 로그인이 가능합니다.변경하시겠습니까?", function(result) {
                    if (result) {
                        location.href = "/pw_change";
                    }
                });
            }
        </script>

</body>

</html>
